#include "calculation.h"


Coordinates* test(Coordinates coordstart, Coordinates ) 
int verif = 0;

while(verif != 0)
{
  Coordinates coordbary = barycentre(coordstart,coordend); 
  double codir = coefdir(coordstart,coordend);
  Coordinates interup =       
}